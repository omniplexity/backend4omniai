apiVersion: v1
kind: Service
metadata:
  name: omniai-backend
  namespace: omniai
  labels:
    app: omniai-backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: omniai-backend
