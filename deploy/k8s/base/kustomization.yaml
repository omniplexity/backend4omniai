apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: omniai

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: omniai
  app.kubernetes.io/component: backend
  app.kubernetes.io/part-of: omniai

images:
  - name: omniai/backend
    newTag: "1.0.0"
